# Module Configuration File
# This file is processed by Maven resource filtering during build

# Module A Configuration
moduleA.enabled=${moduleA.enabled}

# Module B Configuration
moduleB.enabled=${moduleB.enabled}

# Version suffix for build identification
version.suffix=${version.suffix}